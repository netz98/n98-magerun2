{% import "dev/console/make/_table_macros.twig" as table_macros %}
$table = $installer->getConnection()->newTable(
    $installer->getTable('{{ table.name }}')
)
{% for column in table.columns %}
    {{- table_macros.render_column(column) }}
{%- endfor -%}
->setComment('{{ table.comment }}');